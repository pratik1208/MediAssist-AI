[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py *_test.py
markers =
    live_model: makes real AI provider calls; excluded by default, run with: pytest -m live_model
addopts = -m "not live_model"
