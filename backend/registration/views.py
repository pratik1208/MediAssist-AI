# Registration flow endpoints (Phase 3): start registration, submit
# demographics, request/verify OTP, upload document, submit insurance,
# get registration status. Generic Patient/Conversation/Message CRUD
# lives in core.views.
from django.shortcuts import render

# Create your views here.
